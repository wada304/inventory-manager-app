import React from 'react'
import ReactDOM from 'react-dom/client'
import InventoryManager from './inventory-manager.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <InventoryManager />
  </React.StrictMode>
)
